GPU_ID: '0'
WORKERS: 1
CUDA: True
LOGDIR: '1_256_3layers_faster'
COMET_PROJECT_NAME: '3transformer-gqa'

TRAIN:
    FLAG: True
    LEARNING_RATE: 0.00005
    BATCH_SIZE: 256
    MAX_EPOCHS: 100
    SNAPSHOT_INTERVAL: 5
    WEIGHT_INIT: "xavier_uniform"
    EALRY_STOPPING: True
    PATIENCE: 5
    CLIP_GRADS: True
    RADAM: False

DATASET:
    DATA_DIR: '/storage1/datasets/GQA/data/'
    DATASET: 'gqa'
    OBJECTS: 'coco'

model:
    d_model: 256
    n_instructions: 1
    transformer_nlayers: 3
    transformer_dropout: 0.2
    transformer_heads: 4
    PE_dropout: 0.1
    in_channels: 2048
    n_operations: 1