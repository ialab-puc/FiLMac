GPU_ID: '0'
WORKERS: 1
CUDA: True
LOGDIR: '3transformer_clevr'
COMET_PROJECT_NAME: '3transformer-clevr'

TRAIN:
    FLAG: True
    LEARNING_RATE: 0.00005
    BATCH_SIZE: 128
    MAX_EPOCHS: 100
    SNAPSHOT_INTERVAL: 5
    WEIGHT_INIT: "xavier_uniform"
    EALRY_STOPPING: False
    PATIENCE: 5
    CLIP_GRADS: True
    RADAM: True

DATASET:
    DATA_DIR: '/storage1/datasets/CLEVR_v1.0/features'
    DATASET: 'clevr'

model:
    d_model: 256
    n_instructions: 8
    transformer_nlayers: 5
    transformer_dropout: 0.2
    transformer_heads: 4
    PE_dropout: 0.1
    in_channels: 1024
    n_operations: 1